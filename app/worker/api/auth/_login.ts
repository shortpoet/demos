// if (url.pathname.startsWith("/api/login")) {
//   await new Auth(env).login();
// }
// if (url.pathname.startsWith("/api/callback")) {
//   return await new Auth().callback();
// }
// if (url.pathname.startsWith("/api/validate")) {
//   return await new Auth(env).validate(
//     request,
//     ValidationModes.Body,
//     ProvidersLiterals._
//   );
// }
